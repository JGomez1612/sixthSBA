// Imports
import express from 'express'

// Setup
const app = express();
const PORT = 3000;

// Middleware

// Routes

//Global Error Handling Middleware

// Server Listener
app.listen(PORT, () => {
    console.log(`Server running on PORT: ${PORT}`)
});